{
  "name": "image-resizer",
  "version": "1.0.0",
  "description": "resize and watermark images",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "npm i --only=dev && cd frontend && npm i --only=dev && npm i && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "benny h",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-formidable": "^1.2.0",
    "image-size": "^0.8.3",
    "jimp": "^0.9.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/node": "^7.8.3",
    "@babel/preset-env": "^7.8.3"
  }
}
